package ZipcodeZB;

import InterfaceExamples.Point;

public class Distance {

    //Comupute the distance between the current zipcode and
    double distance(Point p);

    static double distance(Point p1,Point p2){
        return p1.distance(p2);
    }
}
